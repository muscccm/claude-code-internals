# Claude Code 提示词与工具详细总结

> 基于 2026-07-17 的 234 条 Claude Code 请求（cc_version=2.1.209）。
> 原文见各组文件夹（`*-zh.md` 为中文译文）。本文档详细解释每段提示词、每个工具的**具体用途**。

---

## 一、Claude Code 主系统提示词（02/03/04 组，约 11.4K 字符）

每次真实对话请求都会完整携带这段提示词。它回答三个问题：模型是谁、能用什么工具、
做事的规矩是什么。逐章详解：

### 1. 开头身份与安全红线

- `You are Claude Code, Anthropic's official CLI for Claude, running within the Claude Agent SDK.`
  ——声明身份：这不是普通聊天机器人，而是一个**能操作电脑干活的编程代理**。
- 安全条款具体规定了：授权的安全测试/CTF/教学场景可以协助；**拒绝**破坏性技术、
  DoS 攻击、批量打击、供应链投毒、恶意规避检测；C2 框架、凭据测试、漏洞开发这类
  双刃剑工具必须有明确的授权背景（渗透测试合同、CTF 比赛、防御研究）。

### 2. Harness（输出渲染规则）

告诉模型"你说的话会怎么被用户看到"：

- 纯文本输出会渲染成 GitHub 风格 markdown——所以可以用标题、表格、代码块排版；
- 工具调用用户默认看不到细节，**重要结论必须写进最后一轮文字里**，不能只留在工具调用过程中；
- 引用代码用 `文件路径:行号` 格式，因为在终端里可以点击跳转；
- 有专用工具时优先用专用工具（比如用 Grep 而不是 `bash grep`），因为权限 UI 对专用工具的展示更友好；
- 多个互不依赖的工具调用应该在同一个回复里并行发出，节省时间。

### 3. Communicating with the user（沟通风格）

这一章决定了 Claude Code"说话的方式"：

- **先给结论**：第一句话就回答"结果是什么/发现了什么"，细节放后面；
- 写完整句子，不堆缩写和 `A → B → 失败` 这种箭头链；
- 用户是专家就简洁些，是新手就多解释；
- 提到任何人且不知道其代词时，一律用 they/them（避免猜错性别）；
- **难以逆转或对外的操作先确认**（发外部服务=可能被缓存索引，删东西前先看清楚目标）；
- 如实报告：测试失败就说失败并附输出，跳过的步骤要说明，做完了就直说做完了不绕弯。

### 4. Memory（持久记忆系统）

Claude Code 有一套跨会话的文件记忆：

- 存在固定目录下，每条记忆一个 markdown 文件，带 frontmatter（name/description/type）；
- 四种类型：**user**（用户是谁、偏好）、**feedback**（用户纠正过的工作方式，要带原因）、
  **project**（进行中的工作、目标、约束，相对日期要转成绝对日期）、**reference**（外部资源链接）；
- `MEMORY.md` 是索引，每次会话开始自动加载——所以只放一行指针，正文放各文件里；
- 明确**不该记的**：代码结构、git 历史、CLAUDE.md 里已有的东西（仓库自己会说话）、只对本次对话有用的东西；
- 记忆之间用 `[[name]]` 互相链接。

### 5. Environment（环境注入）

每次请求都附带当前运行环境，让模型生成的命令/路径符合实际：

- 主工作目录、是否 git 仓库、平台、shell、OS 版本；
- 当前模型 ID（`claude-fable-5[1m]`）、知识截止日期（2026-01）、最新模型列表
  （告诉模型"你自己是最新的 Fable 5"，避免它自称旧型号）；
- 当前日期（2026-07-17）——搜索时效性内容时用。

### 6. Context management（上下文管理）

告诉模型：对话变长时系统会自动做摘要并续接，**不用提前收尾、不用急着交接**；
上下文够就直接干，别重复推导已知结论、别重新争论用户已定的决策。

### 7. 自主运行规则（很重要的一章）

- 用户**不在实时观看**，问"要不要我做……？"只会卡住工作——可逆且符合原始请求的操作直接做；
- 只有破坏性操作或真正的范围变更才停下来问；
- **例外**：用户只是在描述问题/提问/自言自语时，交付物是"你的评估"——报告发现就停，别擅自修；
- 结束前检查最后一段：如果是计划、承诺、"我待会儿会……"，那就现在做完。

### 8. Session-specific guidance

用户输入 `/技能名` 时通过 Skill 工具调用，且只能用"可用技能列表"里列出的，不能猜名字。

### 9. 版本变体（03/04 组）

与 02 组逐字相同，仅：03 的版本戳是 `cc_version=2.1.209.74d`（02 是 `89f`）；
04 的模型 ID 写 `claude-fable-5`（02 写 `claude-fable-5[1m]`）。
即**不同构建的客户端发的提示词内容完全一致**，差异只是构建元数据。

---

## 二、58 个工具逐个详解（02 组 tools.md）

工具定义（名称+描述+JSON Schema）每次请求原样发送，共约 124K 字符。
58 个工具 = 11 核心 + 19 扩展 + 28 MCP + 1 个 Skill（与探针分组完全对应）。

### 2.1 核心内置工具（11 个，探针组 09）

| 工具 | 具体用途 |
|------|---------|
| **Agent** | 派生子代理处理复杂多步任务。子代理有自己的上下文窗口，在后台运行，完成后把结论返回给主会话。支持 worktree 隔离（在独立 git 工作区里改文件，避免并行冲突）、远程运行。适合：并行做多件独立的事、需要读大量文件但只要结论的场景 |
| **AskUserQuestion** | 当真的只有用户能拍板时，弹出结构化选择题（2-4 个选项，可多选，可带 ASCII 预览图）。规则：有常规默认值的事不许问 |
| **Bash** | 执行 shell 命令（本机是 Git Bash）。支持超时（最长 10 分钟）、后台运行（命令持续运行，退出时通知）。git 操作、运行测试、启动服务都靠它 |
| **Edit** | 精确字符串替换改文件。必须先 Read 过该文件；`old_string` 必须唯一匹配，防止误改 |
| **Glob** | 按文件名模式找文件（如 `**/*.ts`），按修改时间排序返回 |
| **Grep** | 基于 ripgrep 的内容搜索。支持正则、上下文行数、文件类型过滤、多行匹配 |
| **Read** | 读文件：文本默认 2000 行；图片直接"看"；PDF 按页范围读；Jupyter notebook 按 cell 读 |
| **ReportFindings** | 代码审查专用：把审查发现按严重度排序、结构化上报给宿主 UI 渲染 |
| **Skill** | 调用技能（slash command）。是**阻塞性要求**：匹配到技能必须先调用再回答 |
| **Workflow** | 多代理编排引擎：写一段 JS 脚本确定性地调度几十个子代理（fan-out 并行、pipeline 流水线、对抗性验证、评委打分）。带 token 预算控制、断点续跑。需要用户明确授权才可用（"ultracode"关键词或明确要求工作流） |
| **Write** | 创建或整体覆写文件。局部修改用 Edit，新建/全量替换用 Write |

### 2.2 扩展内置工具（19 个，探针组 06）

| 工具 | 具体用途 |
|------|---------|
| **CronCreate / CronDelete / CronList** | 定时提示词：按 cron 表达式在未来某个时间自动给 Claude 发一条提示（如"每天早上 9 点检查收件箱"）。durable 的写盘持久化，会话级的重启即失。循环任务 7 天自动过期 |
| **DesignSync** | 与 claude.ai 的设计系统项目同步：读取远程项目文件、锁定写入计划（finalize_plan）、上传/删除文件、注册组件预览卡片。用于维护组件库 |
| **EnterPlanMode / ExitPlanMode** | 计划模式：做非平凡改动前先进入计划模式探索代码库、设计实现方案，写出计划文件后由用户审批，批准后才动手写代码 |
| **EnterWorktree / ExitWorktree** | git worktree 隔离：创建独立工作区+新分支干活，不污染主目录；退出时可保留或删除 |
| **NotebookEdit** | 替换/插入/删除 Jupyter notebook 的单个 cell |
| **ScheduleWakeup** | `/loop` 动态模式的自我定时：安排多少秒后唤醒自己继续下一轮循环（如每 20 分钟检查一次 CI）。选延迟时要考虑 prompt 缓存窗口（5 分钟内缓存还热） |
| **SendMessage** | 给另一个命名子代理发消息继续协作（子代理完成后还能被唤醒接着干） |
| **TaskCreate / TaskGet / TaskList / TaskUpdate / TaskStop** | 任务清单系统：把复杂工作拆成带状态（pending/in_progress/completed）和依赖关系（blocks/blockedBy）的任务，跟踪进度；TaskStop 可强制停止后台任务 |
| **WebFetch** | 抓网页：URL 转成 markdown 后用一个小快模型按给定问题提取答案。不能用于需要登录的私有页面 |
| **WebSearch** | 联网搜索（限美国节点），返回标题+URL，可按域名过滤 |

### 2.3 MCP 工具（28 个，探针组 08）

MCP（Model Context Protocol）工具由外部服务提供，分三组：

**ccd 会话辅助（3 个）**——Claude 桌面应用提供的会话内能力：

| 工具 | 具体用途 |
|------|---------|
| `mcp__ccd_directory__request_directory` | 请求访问工作目录之外的文件夹（弹系统目录选择器让用户授权） |
| `mcp__ccd_session__mark_chapter` | 在会话时间线上打一个"章节"标记（如"代码探索"→"修复 bug"→"验证"），用户可按章节跳转 |
| `mcp__ccd_session__spawn_task` / `dismiss_task` | 发现范围外的问题时，生成一个"任务卡片"浮在 UI 上，用户一键就能另开会话处理；`dismiss_task` 撤回过时的卡片 |

**ccd 会话管理（7 个）**——管理用户的其他 Claude 会话：

| 工具 | 具体用途 |
|------|---------|
| `list_sessions` / `get_session` | 列出/查看其他会话的元数据（标题、分支、PR、模型、创建时间） |
| `list_events` / `search_session_transcripts` | 读另一个会话的对话记录 / 全文搜索所有会话的历史消息（找"上次哪个会话讨论过 X"） |
| `send_message` | 给另一个会话发消息（把上下文交接过去，需用户确认） |
| `set_session_title` | 重命名会话 |
| `archive_session` | 归档会话（停进程、清 worktree，可从归档列表恢复） |

**浏览器预览（14 个，`mcp__Claude_Browser__preview_*`）**——前端开发调试：

| 工具 | 具体用途 |
|------|---------|
| `preview_start` / `preview_stop` / `preview_list` | 按 `.claude/launch.json` 配置启动/停止/列出开发服务器 |
| `preview_screenshot` / `preview_snapshot` | 截图看布局 / 抓无障碍树（精确的文本和元素结构，比截图更适合验证内容） |
| `preview_inspect` | 检查 DOM 元素的计算样式（颜色、字号、间距的精确值） |
| `preview_click` / `preview_fill` | 点击元素 / 填表单 |
| `preview_eval` | 在页面里执行 JS（仅限调试读取，不许用它实现功能改动） |
| `preview_console_logs` / `preview_logs` / `preview_network` | 读浏览器控制台 / 服务器 stdout / 网络请求 |
| `preview_resize` | 改视口尺寸测响应式布局，可模拟深色模式 |

**定时任务（4 个，`mcp__scheduled-tasks__*`）**——应用打开时自动运行的任务：

| 工具 | 具体用途 |
|------|---------|
| `create_scheduled_task` | 创建定时任务：cron 循环或 fireAt 一次性。任务提示词必须自包含（每次运行都是全新会话，没有本次对话的记忆） |
| `update_scheduled_task` | 改任务的提示词/日程/启用状态 |
| `list_scheduled_tasks` / `delete_scheduled_task` | 列出（含下次运行时间）/ 删除任务 |

### 2.4 Skill 工具（单独成组，探针组 05）

调用"技能"的入口。技能是预定义的领域能力包（一组指令+资源），用户输入 `/名字`
或任务匹配时调用。它的描述里嵌着当前可用技能列表，**技能增减时描述会变**，
所以 Claude Code 把它从工具集里拆出来单独测量 token——这就是 05 组只有 1 个工具的原因。

---

## 三、Claude Agent SDK 通用代理提示词（01 组，27K 字符）

只出现 1 次、不带工具的"大号"提示词，开头是
"You are a Claude agent, built on Anthropic's Claude Agent SDK"（不是 "Claude Code"）。
其环境段落显示工作目录为 `…\AppData\Local\Claude-3p\title-gen\title-gen-<id>\`——
**这是桌面应用为自动生成会话标题而派生的独立 Agent SDK 进程**，每次新会话跑一次，
所以只抓到 1 条请求、且不需要任何工具。它比主提示词更详尽，各章用途：

| 章节 | 具体作用 |
|------|---------|
| System | 系统级总纲：代理的定位和基本行为边界 |
| Doing tasks | 任务执行方法论：怎么拆解任务、怎么验证结果、什么时候算"完成" |
| Executing actions with care | 谨慎执行清单：哪些操作算"难以逆转"（删数据、发外部请求、改共享状态），执行前要怎么确认 |
| Using your tools | 工具使用纪律：优先专用工具、并行调用、失败处理 |
| Tone and style / Text output | 语气与排版规则（与 02 组类似但更细） |
| auto memory | 记忆系统**完整规范**，比 02 组详细得多：四种记忆类型分别举了正例反例；明确列出"不该存"的清单（代码结构、git 历史、临时上下文）；引用记忆前必须先验证（提到文件路径要检查存在、提到函数/开关要先 grep）；还讲了记忆与其他持久化形式（CLAUDE.md、注释、文档）的边界 |
| Environment | 这组的环境是 `cmd.exe`（02 组是 bash）——说明来自**不同的会话/shell 配置** |
| Context management | 上下文压缩与续接的说明 |

**与 02 组的关系**：两者骨架相同（同一套代理理念），01 更偏"通用代理单轮任务"，
02 更偏"Claude Code 编程场景"。01 组的发现说明：桌面应用的**会话标题也是由模型生成的**，
而且会先完整加载一套系统提示词 + 记忆系统再生成标题。

---

## 四、token 计数探针详解（05–09 组，占全部请求的 60%）

### 原理

```
请求：max_tokens=1 + 要测量的内容
响应：usage.input_tokens = 内容的精确 token 数
```

只让模型输出 1 个 token（成本几乎为零），就能从响应的 usage 里读出**输入部分的精确
token 数**——相当于用 API 本身当"秤"。

### 为什么分组测量

Claude Code 的 `/context` 命令会显示上下文构成（系统提示词 X tokens、工具 Y tokens、
对话历史 Z tokens、剩余空间多少）。要给出这个分解，就得**分别称出每个部分的重量**：

| 探针组 | 称的是什么 |
|--------|-----------|
| 09（11 工具） | 核心工具集的 token 量 |
| 06（19 工具） | 扩展工具集的 token 量 |
| 08（28 工具） | MCP 工具集的 token 量 |
| 05（1 工具） | Skill 工具（描述动态变化，需频繁重称） |
| 07（123 条） | 零散文本片段：session guidance 提醒块、系统提醒等动态注入的内容 |

### 07 组补充

123 条里绝大多数是上述片段探测（`max_tokens=1`），另有少数 `max_tokens=16` 的
是手工测试请求（user-agent 是 `curl/8.21.0`）。

---

## 五、Skill 注入机制（skills.md）

技能内容**不在工具定义里**，而是以 system-reminder 的形式混在消息流中：

- 会话开始或技能列表变化时，消息流里插入"可用技能列表"提醒块（名称+描述+何时使用）；
- 用户输入 `/技能名` → Skill 工具调用 → 技能的完整指令作为工具结果返回；
- skills.md 保存了抓取到的这些提醒块原文（02/03/04 组各一份，内容随会话进展略有不同）。

---

## 六、一次真实对话请求的完整构成

以本会话的一次请求（约 300KB）为例，发出去的 JSON 里有什么：

| 字段 | 内容 | 量级 |
|------|------|------|
| `system` | 第二章详解的主提示词 | 11.4K 字符 |
| `tools` | 第三章详解的 58 个工具定义 | 124K 字符 |
| `messages` | 完整对话历史（你的消息、我的回复、每次工具调用和结果） | 大头，随对话增长 |
| `model` / `max_tokens` / `stream` | 客户端配置的模型名 / 64000 / true | — |
| `thinking` + `output_config` | `{"type":"adaptive"}`（自适应思考模式）+ `{"effort":"max"}`（思考强度档位） | — |
| `metadata.user_id` | 设备 ID + 会话 ID 的 JSON 字符串 | — |

这就是为什么每次请求都是几百 KB：**提示词和工具定义是固定开销，对话历史是增长开销**，
也是 token 探针要持续监控上下文大小的原因。
